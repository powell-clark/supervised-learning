# IVM snapshot

Machine-written. The identity, vision and mission text as of the last vow derivation,
kept so a substantive edit can be detected at session start (TASK-CCC29115 D-5).
Do not hand-edit: replacing this with the current IVM silently suppresses the
re-derivation prompt for a change that has already happened.

---
Powell-Clark Limited's open supervised-learning curriculum: a series of Jupyter notebooks that teach machine learning from mathematical first principles, one algorithm family per lesson, each lesson a theory notebook (derivations plus a from-scratch NumPy implementation verified numerically) paired with a practical notebook (production libraries applied to a real dataset, cross-checked against the from-scratch version). The voice is academic and plain: no emojis, no marketing language, no tool tutorials. Copyright Powell-Clark Limited, Apache 2.0, free to read and run in Colab.

A university-grade, self-contained corpus that a reader with school calculus can work through end to end and come out able to derive, implement and apply every core supervised learning method — the foundation layer of a wider series spanning unsupervised learning, reinforcement learning and modern deep learning, all built to the same standard and all readable as finished, executed notebooks rather than code that has to be trusted.

Complete and maintain the supervised-learning corpus at the bar set in CURRICULUM_ROADMAP.md — every theory notebook with 100+ LaTeX derivation steps and a gradient- or numerically-checked from-scratch implementation, every practical notebook with 20+ mathematical explanations and a library-versus-from-scratch comparison, every notebook executed with outputs stored and zero errors — and do so through an autonomous, mechanically verified build in which agents plan, write, check and close the work, so that the operator's only job is to read the result.
